ЗАДАЧА: Реализовать механику Broker Introduction + контроль сделки + ограниченный доступ к данным яхт + Deal Room с approval

У МЕНЯ УЖЕ ЕСТЬ:
- Supabase (Auth, users с role и approved)
- таблица yachts
- роли: investor, broker, owner, admin

-----------------------------------
1. ОБНОВИТЬ ТАБЛИЦУ YACHTS
-----------------------------------

Добавить поля:

ALTER TABLE yachts ADD COLUMN IF NOT EXISTS is_locked BOOLEAN DEFAULT true;
ALTER TABLE yachts ADD COLUMN IF NOT EXISTS public_name TEXT;
ALTER TABLE yachts ADD COLUMN IF NOT EXISTS length TEXT;
ALTER TABLE yachts ADD COLUMN IF NOT EXISTS year TEXT;
ALTER TABLE yachts ADD COLUMN IF NOT EXISTS builder TEXT;
ALTER TABLE yachts ADD COLUMN IF NOT EXISTS main_image TEXT;

ВАЖНО:

В ПУБЛИЧНОМ ДОСТУПЕ показывать ТОЛЬКО:
- builder (производитель)
- length (размер)
- year (год)
- main_image (1 фото)

ВСЁ ОСТАЛЬНОЕ:
- скрыто

-----------------------------------
2. ТАБЛИЦА REQUESTS (КЛЮЧЕВАЯ)
-----------------------------------

Создать:

CREATE TABLE IF NOT EXISTS access_requests (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  yacht_id UUID REFERENCES yachts(id) ON DELETE CASCADE,
  requester_id UUID,
  role TEXT,
  status TEXT DEFAULT 'pending', -- pending / approved / rejected
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-----------------------------------
3. ТАБЛИЦА INTRO LOG
-----------------------------------

CREATE TABLE IF NOT EXISTS introductions (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  yacht_id UUID REFERENCES yachts(id),
  from_user UUID,
  to_user UUID,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-----------------------------------
4. ПУБЛИЧНАЯ СТРАНИЦА YACHTS
-----------------------------------

Изменить UI:

Показывать ТОЛЬКО:

- builder
- length
- year
- main_image

УБРАТЬ:
- цена
- описание
- location
- owner
- broker

Кнопка:

👉 "Request Full Details"

-----------------------------------
5. ЛОГИКА REQUEST ACCESS
-----------------------------------

При клике:

- создаётся запись в access_requests
- status = pending

Показать:

"Request submitted. Awaiting approval."

-----------------------------------
6. ADMIN PANEL
-----------------------------------

Добавить страницу:

src/pages/AdminRequests.tsx

Функционал:

- список access_requests
- email пользователя
- яхта
- кнопки:

[Approve] [Reject]

При approve:
- status = approved

-----------------------------------
7. ДОСТУП К ПОЛНОЙ ИНФОРМАЦИИ
-----------------------------------

Если:

request.status !== approved

👉 НЕ показывать:

- цену
- описание
- документы
- контакт

Если approved:

👉 открыть:

- все данные яхты
- кнопка "Enter Deal Room"

-----------------------------------
8. DEAL ROOM ЛОГИКА
-----------------------------------

Создать:

DealRoom.tsx
DealDetails.tsx

Показывать:

- deal_price
- market_price
- описание сделки
- документы

НО:

если нет approved request:

👉 "Restricted Access"

-----------------------------------
9. DOCUMENT ACCESS CONTROL
-----------------------------------

Документы:

- показывать ТОЛЬКО если request approved
- использовать Supabase Storage

-----------------------------------
10. BROKER INTRODUCTION МЕХАНИКА
-----------------------------------

Добавить кнопку:

👉 "Request Introduction"

При клике:

- создаётся запись в introductions
- from_user = текущий
- to_user = владелец листинга (или broker)

Показать:

"Our team will connect you"

-----------------------------------
11. УБРАТЬ ПРЯМЫЕ КОНТАКТЫ
-----------------------------------

НИГДЕ не показывать:

- email брокера
- телефон

Всё через платформу

-----------------------------------
12. RLS (ВАЖНО)
-----------------------------------

- только approved users могут видеть данные
- только владелец или admin может видеть requests
- запретить anon доступ

-----------------------------------
13. UX
-----------------------------------

Добавить статусы:

- Pending → "Under review"
- Approved → "Access granted"
- Rejected → "Request declined"

-----------------------------------
РЕЗУЛЬТАТ:

Я хочу:

1. Публичный каталог (ограниченный)
2. Request access
3. Admin approval
4. Unlock данных
5. Deal Room
6. Intro между брокерами
7. Полный контроль доступа